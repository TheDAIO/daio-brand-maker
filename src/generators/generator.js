// Generator interface
// generateIconCandidates({ prompts: string[], size: number }) => Promise<{ images: Array<{png: Buffer, prompt: string}> }>

export class Generator {
  /** @param {{prompts: string[], size: number}} _opts */
  async generateIconCandidates(_opts) {
    throw new Error('Not implemented');
  }
}
